<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Car</title>
    <style>
        /* Style général */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f7f7f7;
            padding: 20px;
        }

        /* Titre */
        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #333;
        }

        /* Tableau de détails */
        table {
            width: 70%;
            margin: 0 auto 30px auto;
            background-color: #fff;
            border-collapse: collapse;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        th, td {
            padding: 15px 20px;
            text-align: left;
            border-bottom: 1px solid #eee;
        }

        th {
            background-color: #007bff;
            color: white;
            text-transform: uppercase;
            font-size: 14px;
            letter-spacing: 0.05em;
        }

        tr:last-child td {
            border-bottom: none;
        }

        /* Liens d'action */
        .action-links {
            text-align: center;
            margin-top: 20px;
        }

        .action-links a,
        .action-links form button {
            display: inline-block;
            margin: 5px 10px;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            border-radius: 6px;
            transition: background-color 0.3s, transform 0.2s;
        }

        /* Bouton retour */
        .back-link {
            background-color: #17a2b8;
            color: white;
        }

        .back-link:hover {
            background-color: #138496;
            transform: translateY(-2px);
        }

        /* Bouton edit */
        .edit-link {
            background-color: #ffc107;
            color: black;
        }

        .edit-link:hover {
            background-color: #e0a800;
            transform: translateY(-2px);
        }

        /* Bouton delete */
        .delete-form button {
            background-color: #dc3545;
            color: white;
            border: none;
            cursor: pointer;
        }

        .delete-form button:hover {
            background-color: #c82333;
            transform: translateY(-2px);
        }
    </style>
</head>
<body>

<h1>Car</h1>

<table class="table">
    <tbody>
    <tr>
        <th>Id</th>
        <td>{{ car.id }}</td>
    </tr>
    <tr>
        <th>Name</th>
        <td>{{ car.name }}</td>
    </tr>
    <tr>
        <th>Address</th>
        <td>{{ car.address }}</td>
    </tr>
    <tr>
        <th>Image</th>
        <td>{{ car.image }}</td>
    </tr>
    <tr>
        <th>Price</th>
        <td>{{ car.price }}</td>
    </tr>
    </tbody>
</table>

<div class="action-links">
    <a href="{{ path('app_car_index') }}" class="back-link">Back to list</a>
    <a href="{{ path('app_car_edit', {'id': car.id}) }}" class="edit-link">Edit</a>
    <div class="delete-form" style="display: inline-block;">
        {{ include('car/_delete_form.html.twig') }}
    </div>
</div>

</body>
</html>

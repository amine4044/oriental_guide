{% extends 'base.html.twig' %}

{% block title %}Modifier l'Hôtel{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card shadow-lg border-0 p-4">
            <h2 class="mb-4 text-center">Modifier l'Hôtel</h2>

            {{ include('hotel/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <div class="d-flex justify-content-between mt-4">
                <a href="{{ path('app_hotel_index') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Retour à la liste
                </a>

                {{ include('hotel/_delete_form.html.twig', { 'button_class': 'btn btn-danger' }) }}
            </div>
        </div>
    </div>
{% endblock %}

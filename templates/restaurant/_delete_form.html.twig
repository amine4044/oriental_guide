<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Delete Button Form</title>
    <style>
        /* Style général du formulaire */
        form {
            display: inline-block;
            margin: 10px 0;
        }

        /* Style du bouton delete */
        form .btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: bold;
            color: #ffffff;
            background-color: #dc3545; /* Rouge classique */
            border: none;
            border-radius: 6px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s, transform 0.2s;
        }

        /* Effet au survol du bouton */
        form .btn:hover {
            background-color: #c82333; /* Rouge plus foncé */
            transform: translateY(-2px);
        }

        /* Effet lors du clic */
        form .btn:active {
            background-color: #bd2130;
            transform: translateY(0);
        }

        /* Si jamais bouton est désactivé */
        form .btn:disabled {
            background-color: #e0aeb0;
            cursor: not-allowed;
        }

        /* Responsive pour mobiles */
        @media (max-width: 600px) {
            form .btn {
                width: 100%;
                padding: 12px;
            }
        }
    </style>
</head>
<body>

<form method="post" action="{{ path('app_restaurant_delete', {'id': restaurant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurant.id) }}">
    <button class="btn">
        🗑️ Delete
    </button>
</form>

</body>
</html>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center mt-3">
        <button class="btn btn-primary py-3 px-4">{{ button_label|default('Sauvegarder') }}</button>
    </div>
{{ form_end(form) }}